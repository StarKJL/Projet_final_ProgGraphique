<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ProjetFinal.AfficherProjets"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ProjetFinal.Classe"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <GridView
    x:Name="gvProjets">
        <GridView.ItemTemplate>
            <DataTemplate x:DataType="local:Projet">
                <Grid
                    Width="400px"
                    Height="100px"
                    CornerRadius="20"
                    Padding="10"
                    Margin="5"
                    Background="LightBlue">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0">
                        <Run Text="{x:Bind NoProjet}"/>
                        <Run Text="|"/>
                        <Run Text="{x:Bind NomClient}"/>
                    </TextBlock>
                    <TextBlock Grid.Row="0" HorizontalAlignment="Right" Text="{x:Bind Statut}"/>

                    <TextBlock Grid.Row="1" HorizontalAlignment="Center" FontSize="20" Text="{x:Bind Titre}"/>

                    <TextBlock Grid.Row="2" HorizontalAlignment="Center" Text="{x:Bind Description}"/>

                    <TextBlock Grid.Row="3" Text="{x:Bind DateDebut}"/>
                    <TextBlock Grid.Row="3" HorizontalAlignment="Center">
                        <Run Text="{x:Bind Budget}"/>
                        <Run Text="$"/>
                    </TextBlock>
                    <TextBlock Grid.Row="3" HorizontalAlignment="Right">
                        <Run Text="{x:Bind TotalSalaire}"/>
                        <Run Text="$"/>
                    </TextBlock>
                </Grid>
            </DataTemplate>
        </GridView.ItemTemplate>

    </GridView>
</Page>
