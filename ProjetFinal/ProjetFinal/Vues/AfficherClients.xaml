<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ProjetFinal.AfficherClients"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ProjetFinal.Classe"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    mc:Ignorable="d">

    <GridView
        x:Name="gvClients">
        <GridView.ItemTemplate>
            <DataTemplate x:DataType="local:Client">
                <StackPanel Orientation="Vertical"
                            Background="LightBlue"
                            Margin="5"
                            Padding="5"
                            Width="300"
                            CornerRadius="10">
                    <TextBlock>
                        <Run Text="Nom:      "/>
                        <Run Text="{x:Bind Nom, Mode=OneWay}"/>
                    </TextBlock>
                    <TextBlock>
                        <Run Text="Adresse: "/>
                        <Run Text="{x:Bind Adresse, Mode=OneWay}"/>
                    </TextBlock>
                    <TextBlock>
                        <Run Text="Email:     "/>
                        <Run Text="{x:Bind Email, Mode=OneWay}"/>
                    </TextBlock>
                    <TextBlock>
                        <Run Text="Tel:         "/>
                        <Run Text="{x:Bind Telephone, Mode=OneWay}"/>
                    </TextBlock>

                    <Grid>
                        <Button x:Name="btnModif" Click="btnModif_Click">Modifier</Button>
                    </Grid>
                </StackPanel>
            </DataTemplate>
        </GridView.ItemTemplate>

    </GridView>
</Page>
