<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ProjetFinal.Vues.AfficherEmployes"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ProjetFinal.Classe"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <GridView
    x:Name="gvEmployes">
        <GridView.ItemTemplate> 
            <DataTemplate x:DataType="local:Employe">
                <StackPanel
                Width="200px"
                Height="300px"
                CornerRadius="20"
                Padding="10"
                Margin="5"
                Background="LightBlue">
                    <TextBlock Text="{x:Bind Matricule}"/>

                    <Image Source="{x:Bind PhotoIdentite}"
                           HorizontalAlignment="Left"
                           Width="100px"/>
                    <TextBlock>
                        <Run Text="{x:Bind Prenom}"/>
                        <Run Text="{x:Bind Nom}"/>
                    </TextBlock>
                    <TextBlock Text="{x:Bind Email}"/>
                    <TextBlock Text="{x:Bind DateNaissance}"/>
                    <TextBlock Text="{x:Bind Adresse}"/>
                    <TextBlock Margin="10"/>
                    <TextBlock Text="{x:Bind TauxHoraire}"/>
                    <TextBlock Text="{x:Bind Statut}"/>


                </StackPanel>
            </DataTemplate>
        </GridView.ItemTemplate>

    </GridView>
</Page>
